################################################################
volumes:
    testserver:
################################################################

################################################################
services:
  study:
    build: 
      context: .
      dockerfile: Dockerfile
    image: study:latest
    ports:
      - 30020:80

    volumes:
      - testserver:/var/www/html/
